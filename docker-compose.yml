version: "3.8"

services:
  github-devlog-ai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: github-devlog-ai
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - .:/app
    command: ["php", "-S", "0.0.0.0:8080", "-t", "public"]
